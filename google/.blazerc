common --color=yes --curses=yes
build -c opt --show_timestamps
test --test_output=errors

# Literal %workspace% (enter as is) references your current google3 workspace.
import %workspace%/javascript/typescript/blazerc
